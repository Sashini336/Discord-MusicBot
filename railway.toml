[build]
builder = "nixpacks"
buildCommand = "npm run build && cd dashboard && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"

[nixpacks]
nodejs = "18"
